name: Hynix 5m Research

on:
  pull_request:
    branches: [main]
    paths:
      - 'hynix_research/**'

permissions:
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install requests numpy pandas matplotlib statsmodels

      - name: Verify analysis script
        run: python -m py_compile hynix_research/datavision_full_report.py

      - name: Download official archives, analyze, and backtest
        env:
          MPLBACKEND: Agg
        run: python hynix_research/datavision_full_report.py --end-date 2026-08-06 --output-dir hynix_5m_report

      - name: Package report
        run: zip -r hynix_5m_report.zip hynix_5m_report

      - name: Upload report artifact
        uses: actions/upload-artifact@v4
        with:
          name: hynix-5m-report
          path: |
            hynix_5m_report.zip
            hynix_5m_report/
          retention-days: 7
          if-no-files-found: error
